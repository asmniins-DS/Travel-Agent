# Travel Agent — Part 2

An LLM-powered travel agent that uses **function/tool calling** to search for flights and process bookings. The agent decides when to call the `search_flights` tool, executes it against a mock API, and continues the conversation with real data.

## Architecture
